# ─── Pedal ELO — environment variables ───────────────────────────────────────
#
# These variables are set in Vercel's dashboard (not in this file).
# This file is only a reference so you know what to add.
#
# In Vercel: Project → Settings → Environment Variables
# Set both for Production, Preview, and Development environments.
#
# NEVER commit real values here — this file is intentionally blank.
# ─────────────────────────────────────────────────────────────────────────────

# Your Upstash Redis REST endpoint (found in Upstash Console → your DB → REST API)
# Example: https://us1-example-12345.upstash.io
UPSTASH_URL=

# Your Upstash Redis REST token (found in the same panel, labelled "Token")
# This value is kept SERVER-SIDE only — it never appears in the browser bundle.
UPSTASH_TOKEN=
